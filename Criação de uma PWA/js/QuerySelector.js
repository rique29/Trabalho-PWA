// Seleciona todos os elementos de número (botões) na calculadora
export let numbers = document.querySelectorAll(".numbers > div");

// Seleciona todos os elementos de operação (botões) na calculadora
export let operations = document.querySelectorAll(".operations > div");

// Seleciona o elemento de exibição da calculadora (visor)
export let phn_display = document.querySelector(".phn_display");
